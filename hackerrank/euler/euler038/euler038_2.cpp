#include <iostream>

using namespace std;

long long int calcAnswer(long long int N, long long int K) {
    
}

int main() {
    long long int N, K;
    cin >> N >> K;
    cout << calcAnswer(N, K) << endl;
    return 0;
}