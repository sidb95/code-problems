#include <iostream>
#include <set>

using namespace std;

long long int calcAnswer(long long int N) {
    
}

int main() {
    int t, T;
    cin >> T;
    for (t = 0; t < T; t += 1) {
        long long int N;
        cin >> N;
        cout << calcAnswer(N) << endl;
    }
    return 0;
}